# TableData reference samples

Copied from repo-root `EDI/TableData/x12/` for comparison and optional UI/file drops.
Story-specific demo fixtures under `samples/` remain the primary happy-path inputs.
Runtime X12 tables for this demo: `278-A1, 278-A3` → `edi-tabledata/<IG>` (5010).
